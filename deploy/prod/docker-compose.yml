version: '3'
services:
  ssr:
    image: "synopticum/urussu-ssr"
  client:
    image: "synopticum/urussu-client"
    ports:
      - '9000:9000'
  nginx:
    depends_on:
      - ssr
      - client
    restart: always
    image: "synopticum/urussu-nginx"
    ports:
      - '80:80'